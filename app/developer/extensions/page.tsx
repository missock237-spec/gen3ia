import { ExtensionsPanel } from "@/components/developer/tabs/extensions-panel";

/** /developer/extensions — versions, permissions, installations. */
export default function DeveloperExtensionsPage() {
  return <ExtensionsPanel />;
}
