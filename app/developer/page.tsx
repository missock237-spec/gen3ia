import { OverviewPanel } from "@/components/developer/tabs/overview-panel";

/** /developer — Vue d'ensemble (barrière serveur dans layout.tsx). */
export default function DeveloperOverviewPage() {
  return <OverviewPanel />;
}
