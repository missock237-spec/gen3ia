import { BuildPanel } from "@/components/developer/tabs/build-panel";

/** /developer/build — Extension / Tool Builder déclaratif. */
export default function DeveloperBuildPage() {
  return <BuildPanel />;
}
