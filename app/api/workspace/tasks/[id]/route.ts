import { NextRequest,NextResponse } from "next/server"; import { requireUser } from "@/lib/security/authenticated-request"; import { errorStatus } from "@/lib/security/http-errors"; import { getWorkspaceTask } from "@/lib/agents/workspace";
export async function GET(request:NextRequest,{params}:{params:Promise<{id:string}>}){try{const user=await requireUser(request);const {id}=await params;return NextResponse.json({task:await getWorkspaceTask(user.uid,id)});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Task not found"},{status:errorStatus(e,404)});}}
